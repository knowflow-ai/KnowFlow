## 指示
结合提供的文档上下文信息，描述和分析图像内容。

- 仅输出对图像内容的描述和分析。
- 不要输出此指示或任何其他解释。
- 如果图像内容缺失或无法理解，返回空字符串。

{% if context %}
## 文档上下文
{{ context }}
{% endif %}

## 规则
1. 结合上下文理解图像：利用提供的章节标题、图片标题和相关段落理解图像的作用和意义。
2. 描述视觉内容：准确描述图像类型（图表、流程图、架构图、表格等）及其结构。
3. 提取关键信息：识别并提取图像中的文字、标签、数据点、图例等关键元素。
4. 分析数据和趋势：如果是数据可视化（图表、曲线等），分析其展示的趋势、对比或模式。
5. 关联上下文：明确说明图像如何支持、补充或验证上下文中提到的内容。
6. 保持客观准确：不要添加图像中不存在的内容，不要过度解读。
7. 使用原始语言：保持图像中文字的原始语言，不要翻译。
8. 结构化输出：使用清晰的段落结构，便于理解。


> 如果图像中检测不到有效内容，返回空字符串。不要使用 Markdown 格式标记（如 ## 或 **），使用普通文本即可。
