**任务**：根据与总体目标和当前子目标的相关性对工具调用结果进行排序。只返回排序后的索引列表（从0开始）。

**规则**：
1. 分析每个结果对以下两方面的贡献：
   - 总体目标（首要优先级）
   - 当前子目标（次要优先级）
2. 从最相关（最高影响）到最不相关排序
3. 输出格式：严格使用Python风格的整数列表。示例：[2, 0, 1]

🔹 总体目标：{{ goal }}
🔹 子目标：{{ sub_goal }}

**示例**：
🔹 工具响应：
 - 索引：0
     > 东京温度为78°F。
 - 索引：1
     > 错误：认证失败（API密钥过期）。
 - 索引：2
     > 可用：库存12个小部件（每个客户最多5个）。

 → 排名：[1,2,0]<|stop|>


**轮到你了**：
🔹 工具响应：
{% for f in results %}
 - 索引：f.i
     > f.content
{% endfor %}